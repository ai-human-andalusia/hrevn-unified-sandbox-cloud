# RWA V1 Foundation

RWA is now a standalone human vertical.

- It reuses the intake skeleton learned from Legacy A.
- It does not depend on the Real Estate SQLite model.
- It has its own SQLite foundation at `app/data/rwa_v1/hrevn_rwa_v1.db`.
- Current schema is intentionally minimal and will be reshaped as fields are defined.
